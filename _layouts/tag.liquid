---
layout: default
---

<header><h1>{{ page.tag }}</h1></header>
{% for post in site.tags[page.tag] %}
  <article>
    <h4><a href="{{ post.url }}">{{ post.title }}</a></h3>
    <h6>{{ post.date | date: "%b %-d, %Y" }}</h5>
    <p>{{ post.excerpt }}</p>
  </article>
{% endfor %}
</ul>
